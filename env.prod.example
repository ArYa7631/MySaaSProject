# Production Environment Variables
# Copy this file to .env.prod and fill in your actual values

# Domain Configuration (No longer needed - supports all domains now)
# SSL certificates are managed per-domain using scripts/add-domain-ssl.sh
# DOMAIN=yourdomain.com
# SSL_EMAIL=your-email@example.com

# Database Configuration
POSTGRES_PASSWORD=your_secure_postgres_password_here

# Rails Secrets
SECRET_KEY_BASE=your_rails_secret_key_base_here
JWT_SECRET_KEY=your_jwt_secret_key_here

# AWS S3 Configuration (for file uploads)
S3_ACCESS_KEY_ID=your_aws_access_key_id
S3_SECRET_ACCESS_KEY=your_aws_secret_access_key
S3_BUCKET=your_s3_bucket_name
S3_REGION=us-east-1

# Optional: Docker Registry (if using private registry)
# DOCKER_REGISTRY=ghcr.io
# DOCKER_USERNAME=your_github_username
# DOCKER_PASSWORD=your_github_token

# Optional: Monitoring and Analytics
# SENTRY_DSN=your_sentry_dsn_for_error_tracking
# GOOGLE_ANALYTICS_ID=your_google_analytics_id
